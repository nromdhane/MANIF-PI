import { EndDateValidatorDirective } from './end-date-validator.directive';

describe('EndDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EndDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
